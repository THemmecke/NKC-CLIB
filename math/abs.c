
int abs(int x)
{
	if (x < 0)
		return -x;
	else
		return x;
}
int __abs__(int x)
{
	if (x < 0)
		return -x;
	else
		return x;
}
long labs(long x)
{
	if (x < 0)
		return -x;
	else
		return x;
}
